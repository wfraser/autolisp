To run AutoLisp:
    $ sbcl --load serv.lisp

To make a binary:
    * edit serv.lisp:
        - comment out the (start-server) at the end
        - uncomment or add in (sb-ext:save-lisp-and-die "serv.bin" :executable t :toplevel 'autolisp:start-server) at the bottom
        - change (start-server) so it ends with (loop (sleep 1000)) -- this prevents returning to the toplevel read-eval-print loop when running the binary.
    $ sbcl --disable-debugger --load serv.lisp
        - this spits out serv.bin, a binary executable

